module.exports = {
	
	
	parensChecker : function(str){
		if(str = "()"){
			return true;
		}
	},
	
	
	// want to index each item in the string
	// want a ) to always follow a ( and if not, it's false
	// want () to be the center
	// cycle through the characters in the string until I get a ) after a (t21`	
		
	
		if (indexOf(str[x] < indexOf(str[x+1]))
	
	// what is the memory value of ( and ) ?
			
	
	see evernote from today for how-to & images :)
	
}